---
title: "API Key Refresh"
icon: "key"
slug: "/refresh-api-key"
---

## Request Endpoint

```python
POST /auth/regenerate-token
```

## Request Headers

<ParamField header="Content-Type" type="string" required>
  Must be set to `application/json`
</ParamField>

## Request Body

<ParamField body="email" type="string" required>
  The email address associated with your account
</ParamField>

<ParamField body="password" type="string" required>
  The current password associated with your account
</ParamField>

## Response Fields

<ResponseField name="api_key" type="string">
  The API key used to authenticate requests to the API.
</ResponseField>

<Note>API keys expire 24 hours after creation and will need to be regenerated.
</Note>

## Example - API Key Request

<CodeGroup>
  ```python
  import requests
  import json

  BASE_URL = ""

  reset_request_data = {
      "email": "name@example.com",
      "password": "SecurePassword2025!"

  }

  headers = {
      "Content-Type": "application/json"
  }

  response = requests.post(
      f"{BASE_URL}/auth/regenerate-token",
      headers=headers,
      data=json.dumps(reset_request_data)
  ).json()
  ```
</CodeGroup>

## Example - Response

<CodeGroup>
  ```python

  {
      "api_key": "stai-abcdefghijklmnopqrstuvwxyz9999456789"
  }

  ```
</CodeGroup>