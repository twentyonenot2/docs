---
title: 'Time Series Model'
icon: 'cloud'
slug: 'ts-model'
---

## Model Details 


<CardGroup cols={2}>
  <Card title="Mandatory Time Periods">
    1 - 501
  </Card>
  <Card title="Optional Time Periods">
    502 - 10,000
  </Card>

</CardGroup>

## Request Header 


<ParamField header="Authorization" type="string" required>
  The authorization token for API requests. This should be a valid Bearer token.
   </ParamField>

## Request Body 

<ParamField body="ticker" type="string" required>
</ParamField>



<ParamField body="interval" type="string" required>

<Expandable title="Options">
    <ParamField name="1m" type="string">
      1 minute time series
    </ParamField>

    <ParamField name="5m" type="string">
      5 minute time series
    </ParamField>

    <ParamField name="1h" type="string">
      1 hour time series
    </ParamField>

    <ParamField name="d" type="string">
      Daily time series
    </ParamField>

    <ParamField name="w" type="string">
      Weekly time series
    </ParamField>

  </Expandable>
</ParamField>



<ParamField body="date_from" type="string" required>
<Expandable title="Format">
    <ParamField name="date_from" type="string">
      YYYY-MM-DD HH:MM:SS
    </ParamField>

  </Expandable>
</ParamField>



<ParamField body="date_to" type="list" required>
<Expandable title="Format">
    <ParamField name="date_to" type="string">
      YYYY-MM-DD HH:MM:SS
    </ParamField>

  </Expandable>
</ParamField>

## Response Fields


<ResponseField name="datetime" type="string">
  Trade opportunity timestamp 
</ResponseField>

<ResponseField name="open" type="number">
Opening price
</ResponseField>


<ResponseField name="current_trend" type="number">
<Expandable title="Responses">
    <ResponseField name="1" type="string">
      Bullish Trend
    </ResponseField>

    <ResponseField name="-1" type="string">
      Bearish Trend
    </ResponseField>

  </Expandable>
</ResponseField>


<ResponseField name="price_trend_forecast" type="number">
<Expandable title="Responses">
    <ResponseField name="1" type="string">
      Bullish Trend
    </ResponseField>

    <ResponseField name="-1" type="string">
      Bearish Trend
    </ResponseField>

    <ResponseField name="0" type="string">
      Uncertain
    </ResponseField>

  </Expandable>
</ResponseField>

<ResponseField name="cumulative_trend_forecast" type="number">
Cumulative price trend forecast for comparison with actual price (Not to Scale)
</ResponseField>

<ResponseField name="risk_forecast_%" type="number">
Expected price movement range (%)
</ResponseField>


<ResponseField name="risk_forecast" type="number">
Expected price movement range (absolute)
</ResponseField>


## Example - API Request

```python

# Import necessary libraries
import requests  
import json  

# Define base URL and access token for the API
BASE_URL = ""  
ACCESS_TOKEN = ""  

# Query parameters for the API request
params = {
    "ticker": "AAPL",
    "interval": "d",
    "date_from": "2020-08-03 00:00:00",
    "date_to": "2024-09-04 00:00:00"
}

# Headers for the API request
headers = {
    "Authorization": f"Bearer {ACCESS_TOKEN}"
}

# API endpoint
api_url = f"{BASE_URL}/genesis/ts-model"

# Make a GET request to the API and parse the JSON response
result = requests.get(api_url).json()
```

## Example - API Response

```python

{
  
  "2020-08-03 00:00:00":
    {
      "open":194.33,
      "current_trend":1,
      "price_trend_forecast":1,
      "risk_forecast_%":-0.2,
      "risk_forecast":193.94
    },

  "2020-08-04 00:00:00":
    {
      "open":194.95,
      "current_trend":1,
      "price_trend_forecast":0,
      "risk_forecast_%":-0.52,
      "risk_forecast":193.94 
    }
  
  ..., 

  "2024-09-03 00:00:00":
    {
      "open":286.35,
      "current_trend":-1,
      "price_trend_forecast":-1,
      "risk_forecast_%":1.1,
      "risk_forecast":289.5 
    },

  "2024-09-04 00:00:00":
    {
      "open":286.55,
      "current_trend":-1,
      "price_trend_forecast":-1,
      "risk_forecast_%":0.9,
      "risk_forecast":289.13
    }
    
}

```
