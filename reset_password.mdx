---
title: 'Reset Password'
icon: 'lock'
slug: '/reset-password'
---

## Request Endpoint

```python
POST /auth/reset-password-request
```

## Request Headers

<ParamField header="Content-Type" type="string" required>
  Must be set to `application/json`
</ParamField>

<ParamField header="Authorisation" type="string" required>
  Authentication token prefixed with "Bearer "
</ParamField>

## Request Body

<ParamField body="email" type="string" required>
  The email address associated with your account
</ParamField>

<ParamField body="old_password" type="string" required>
  The current password associated with your account
</ParamField>

<ParamField body="new_password" type="string" required>
  The new password to associate with your account
</ParamField>

## Response Fields

<ResponseField name="message" type="string">
  Confirmation that the password has been reset
</ResponseField>

## Example - Reset Password Request

```python
import requests
import json

BASE_URL = ""

reset_request_data = {
    "email": "trader@example.com",

}

headers = {
      "Content-Type": "application/json"
      "Authorisation": "Bearer stai-abcdefghijklmnopqrstuvwxyz9999456789"
}

response = requests.post(
    f"{BASE_URL}/auth/reset-password-request",
    headers=headers,
    data=json.dumps(reset_request_data)
).json()
```

## Example - Response

```python

{
    "message": "Password has been reset successfully."
}

```