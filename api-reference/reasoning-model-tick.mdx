---
title: "Tick Data"
description: "This reasoning model identifies directional trends by predicting how likely price action will propagate from tick-level to lower-frequency timeframes, distinguishing structurally significant market moves from noise."
slug: "/api-ref/reasoning-model-tick"
---

## Base URL

```python
 https://www.sumtyme.tech
```

## Request Endpoint

```python
POST /agn-reasoning/tick
```

## OHLC Input Data Lengths

<CardGroup cols="2">
  <Card title="Minimum Length">
    5000 Periods
  </Card>
  <Card title="Maxiumum Length">
    10000 Periods
  </Card>
</CardGroup>

## Request Header

<ParamField header="Content-Type" type="string" required>
  Must be set to `application/json`
</ParamField>

<ParamField header="X-API-Key" type="string" required>
  Your API key is a unique, one-time generated credential that authenticates your requests to our API.
</ParamField>

## Request Body

<ParamField body="timestamp" type="list" required>
  
</ParamField>

<ParamField body="price" type="list" required>
  
</ParamField>


<ParamField body="interval" type="int" required>
    Only the value `1` is allowed.
</ParamField>

<ParamField body="interval_unit" type="string" required>
  <Expandable title="options">
    <ResponseField name="tick" type="string">
      For tick-level data.
    </ResponseField>
  </Expandable>
</ParamField>

<ParamField body="reasoning_mode" type="string" required>
  <Expandable title="Options">
    <ResponseField name="proactive" type="string">
      Act early with minimal information.
    </ResponseField>
    <ResponseField name="reactive" type="string">
      Wait for sufficient evidence before acting.
    </ResponseField>
  </Expandable>
</ParamField>

## Response Fields

<ResponseField name="datetime" type="string">
  Trend timestamp
</ResponseField>

<ResponseField name="trend_identified" type="string">
  <Expandable title="Options">
    <ResponseField name="1" type="int">
      Bullish trend
    </ResponseField>
    <ResponseField name="-1" type="int">
      Bearish trend
    </ResponseField>
  </Expandable>
</ResponseField>

> Important: This endpoint only returns data for datetimes where a trend is identified. If no trend is detected for a specific time period, those datapoints will be omitted from the response. Check the timestamp field in the returned objects to identify which time periods are included.
