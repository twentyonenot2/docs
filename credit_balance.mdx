---
title: "Available Credits"
icon: "ticket"
slug: "/avaialble-credits"
---

Purchase additional API credits [here](https://buy.stripe.com/eVa6qG94ibPRbUAdQQ)

## Request Endpoint

```python
POST /auth/available-credits
```

## Request Headers

<ParamField header="Api-Key" type="string" required>
  The API key used to authenticate requests to the API.
</ParamField>

## Response Fields

<ResponseField name="credit_balance" type="int">
  Remaining API credits
</ResponseField>

## Example - Available Credits Request

<CodeGroup>
  ```python
  import requests
  import json

  BASE_URL = "https://www.sumtyme.com/shared"
  API_KEY = ""

  headers = {
      "Api-Key": f"{API_KEY}"
  }

  response = requests.post(
      f"{BASE_URL}/auth/available-credits",
      headers=headers
      ).json()
  ```
</CodeGroup>

## Example - Response

<CodeGroup>
  ```python

  {
      "credit_balance": 28
  }

  ```
</CodeGroup>