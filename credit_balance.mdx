---
title: "Available Credits"
icon: "ticket"
slug: "/available-credits"
---

Purchase additional API credits [here](https://buy.stripe.com/eVa6qG94ibPRbUAdQQ)

## Base URL

```python
 https://www.sumtyme.com/shared
```

## Request Endpoint

```python
POST /auth/available-credits
```

## Request Headers

<ParamField header="Api-Key" type="string" required>
  The API key used to authenticate requests to the API.
</ParamField>

## Response Fields

<ResponseField name="credit_balance" type="int">
  Remaining API credits
</ResponseField>

## Example - Available Credits Request

<CodeGroup>
  ```python
  # Import necessary libraries
  import requests
  import json

  # Specify Base URL & API KEY
  BASE_URL = ""
  API_KEY = ""

  # Set header for API request
  headers = {
      "Api-Key": f"{API_KEY}"
  }

  # Send POST request to API
  response = requests.post(f"{BASE_URL}/auth/available-credits",
              headers=headers).json()

  ```
</CodeGroup>

## Example - Response

<CodeGroup>
  ```python

  {
      "credit_balance": 28
  }

  ```
</CodeGroup>